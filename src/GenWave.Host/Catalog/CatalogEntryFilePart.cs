namespace GenWave.Host.Catalog;

/// <summary>Which half of a catalog entry a <see cref="CatalogEntryFetchResult"/> failure names (SPEC F90.3).</summary>
public enum CatalogEntryFilePart
{
    Card,
    Meta,
}
