# 📻 GenWave — Shipped Phases

The project's development history, epic by epic — what shipped and why it matters. Newest work
is at the bottom; the current roadmap lives in the [README](README.md#roadmap).

- **v1 broadcast playout** — loudness-matched, crossfaded, never-silent MP3 stream on Docker.
- **Phase 1 — TTS as a source** — station IDs, lead-ins, back-announces, time/date patter interleaved with music via Kokoro (CPU).
- **Epic F — Cue-point trim** — per-track `cue_in_sec`/`cue_out_sec` at enrichment, `liq_cue_in`/`liq_cue_out` stamped at push. Liquidsoap 2.4 honors natively; zero engine-script change. Tightens dead-air gap at music→voice and voice→music transitions; `blank.eat` stays as backstop for un-enriched rows.
- **Admin UI (read-only)** — Next.js operator console (`localhost:3000`): catalog browse, now-playing, play-history. Single-station deployment; config-password auth; no reverse proxy. A second station = a second deployment with its own `.env`.
- **Epic H — Energy-aware transitions** — per-track intro/outro energy measured at enrichment (`intro_energy`/`outro_energy`), stamped as `gw_*_energy` annotations, mapped by the engine's `gw_transition` to a crossfade duration in `[GW_XFADE_MIN, GW_XFADE_MAX]` (default 2–8s): hotter pairs wash shorter, mellow pairs longer; fixed 3s/3s fallback when energy is absent — never silence.
- **Epic I — Catalog writes + station settings** — per-track tag edits and rotation eligibility (incl. bulk by-filter curation for 9000+ tracks), and live non-secret station settings via the Admin UI. `PATCH /api/media/{id}` (`If-Match` optimistic concurrency), `GET/PUT /api/settings`. DB-backed settings overlay (`station.settings` + `station_svc` role) live-applies api-side knobs (loudness target, cadence, crossfade range) without an `api` restart; engine-side knobs (`GW_XFADE_*`) apply on engine restart with a UI warning.
- **Epic J — Library management + re-enrichment** — create/rename/delete libraries, reassign tracks between them (single `PATCH /api/media/{id}` with `libraryId` + bulk `POST /api/media/bulk/reassign`), and operator-triggered re-enrichment (`POST /api/media/{id}/reenrich?fields=…` + `POST /api/media/bulk/reenrich`). Re-enrichment is a sentinel reset — the existing enricher worker reclaims at the 50/tick cap; no new background service. Cross-scope reassigns succeed and signal with `X-Out-Of-Scope: true` + UI confirm. This is what makes `Station:Scope:LibraryIds` a real curation knob.
- **Epic K — Safe-rotation as a curated library scope** — the hand-authored safe file is retired; the engine's `safe` source is a `request.dynamic` pulling annotated tracks from `Station:SafeScope:LibraryIds` via anonymous `GET /internal/safe-track` (core network only). Safe tracks are first-class pushes: loudness matching, cue trim, and energy transitions apply identically. Empty scope → `204` → `mksafe` silence (logged degraded mode).
- **Epic M — Operational hardening** — safe prefetch capped at 1 (`prefetch=1, retry_delay=5.` — scope edits lag at most one stale track), the named-library escape hatch for parked-row recovery (`?library-id=` browse + bulk filters swap effective scope, `X-Out-Of-Scope: true`), `Station:Scope:LibraryIds` live-editable, and full now-playing metadata for engine-initiated safe plays (parsed from the output-metadata poll — no extra telnet, no per-tick DB).
- **Epic N — SafeScope-empty legalization** — an empty `Station:SafeScope:LibraryIds` is legal at boot and via `PUT /api/settings` (WARN + degrade to `mksafe` on drain, per SPEC F4.4); distinct WARN logs separate empty-scope from empty-catalog drains; the settings page badges "Silent on drain." Drain-state display overrides were built, shipped, and reverted after live verification — branding belongs in the authored content's tags, not the transport path.
- **Epic P — Safe-loop authoring** — TTS-generated "we'll be back" safe segments authored from inside the system: sync `POST /api/safe-segments` + an Admin UI "Safe content" page, optional station-jingle bed mixed offline via ffmpeg (cue-trimmed, looped, ducked), artifacts on the `authored` volume as first-class catalog rows branded `artist: <Station Name>` / `title: "Please Stand By"` (embedded in the file too), plus an idempotent boot seed so fresh deploys drain into the announcement. Closes gitea-#149/gitea-#172.
- **Epic Q — Admin UI redesign "Wireless"** — the console's committed identity: warm retro radio (cream/rust/brass), Fraunces + Source Sans 3 vendored via `next/font/local`, light + dark ("walnut & brass") with a persisted cookie toggle, Tailwind v4 + shadcn/ui, token-first theming (a future named theme = one token file — canonical tokens in `.claude/skills/design-aesthetic/SKILL.md`). Persistent sidebar shell + breadcrumbs, a real dashboard (now-playing card, `GET /api/status` tiles, recent plays), Libraries as a Catalog tab, selection-model bulk toolbar, toasts + modal confirms, 5 s polling paused on hidden tabs, responsive to 390 px. Closes gitea-#174.
- **Epic R — Hardening sweep (SPEC F29–F32)** — closes the Epic P/Q operator-testing backlog (gitea-#179–gitea-#186, gitea-#192, gitea-#193): `{StationName}` expansion at the author seam, a distinct boot-seed title, Kokoro voices via `GET /api/voices` + a dropdown with free-text fallback, a configurable engine-side gap between safe tracks (`GW_SAFE_GAP_SECONDS`, default 7 s, `0` disables), guaranteed `artist` on engine-initiated plays (the feeder re-extracts metadata on every advance for engine-owned ids), main scope live via `IOptionsMonitor` (`StationContext` keeps identity only), `ETag` on every PATCH success + one shared row-PATCH hook with real failure toasts, depleted-SafeScope warnings on settings + dashboard, a favicon, and the smoke test formally documented as a **manual pre-release gate**, not CI. Post-ship live fixes on `main`: the favicon is now the operator's GenWave logo (`app/icon.png`), TTS patter carries `artist` = station name, and the feeder always refills at boot (SPEC F7.5 amended — a single-segment safe rotation could deadlock a fresh boot).
- **Epic S — Track rating (SPEC F33)** — an operator taste signal on the Live page: thumbs up/down adjust a per-track score (default 50, clamped 0–100), an X toggles a standalone never-play flag that immediately suppresses every selection path (main, safe, `/media/random`). State lives in a 1:1 `library.media_rating` extension table so votes never disturb edit `ETag`s and bulk curation is structurally incapable of touching ratings. Score is a ledger until the scheduling phase consumes it. Catalog shows score, a never-play badge, a `?never-play=true` filter, and a restore control (no one-way doors). Vote/never-play endpoints are deliberately not scope-gated. Closes gitea-#188 (score-weighted selection deferred to the scheduling phase).
- **Epic T — DJ intelligence (SPEC F34–F36)** — patter graduates from fixed templates to LLM-authored lead-ins/back-announces: an OpenAI-compatible chat-completions endpoint (Ollama-native) behind the new `ISegmentCopyWriter` seam, with the shipped templates as the terminal fallback — any LLM failure means one WARN and yesterday's patter, never a stall. Operator-authored DJ personas (`station.persona`, one live-active via `Station:Persona:ActiveId`) flavor the copy and pick the TTS voice, authored on a Personas page with honest previews (real LLM copy + playable wav — no silent fallback). `Tts:Endpoint`/`Llm:Endpoint` are location-agnostic, live-editable URLs — compose ships unchanged; running either service in-stack or on the LAN is the operator's own deployment choice. Blurb audio is GC'd by `Tts:BlurbRetentionHours`; the dashboard gains an LLM status tile. Closes gitea-#175/gitea-#176/gitea-#178 — plus the live-found gitea-#211 (cadence toggles now apply live via `ICadenceProvider`).
- **Epic U — On-air metadata fidelity (SPEC F37–F40)** — safe plays gain level-matching (the safe branch never had its own `amplify` — audio-real, measured pre/post on a recorded drain) and real `gainDb` telemetry via the encoder metadata-export list (gitea-#200); annotations always stamp `artist` (explicitly empty when the row has none — grounded in a source-verified Liquidsoap v2.4.4 pass; the gitea-#199 bleed itself never reproduced, contingency recorded); all four patter kinds air attributed to the active persona (gitea-#212), and ICY collapses the redundant `"Station | DJ - Station"`; the dashboard SafeScope tile labels tracks vs libraries — no number without a noun (gitea-#214).
- **Epic V — Rotation quality + console fidelity (SPEC F41–F45)** — music selection becomes a tiered preference query (`GetRotationCandidateAsync`): not-recent → artist-not-in-last-N → not-most-recent → random, relaxing with WARNs instead of draining a small catalog to safe (gitea-#210, gitea-#213), with live `Station:Rotation:RecentWindow`/`ArtistSeparation` knobs; `StationIdEveryNUnits` skips unit 0 and `0` disables (gitea-#216); the single-row out-of-scope 403 is repealed — scope is curation, not trust; `X-Out-Of-Scope: true` signals instead (gitea-#203); the settings allowlist completes (every non-secret tunable, four named exclusions; `StationContext` retired for a live `IStationIdentityProvider`; the shell wordmark is the live station name) (gitea-#195–gitea-#197); catalog conflict-retry fixed client-side (gitea-#201/gitea-#202).
- **Epic X — Enrichment 2.0 (SPEC F46–F51)** — BPM analysis via `aubio` in the api image (gitea-#190); whole-track energy as a STORED generated column over the already-measured LUFS — instant catalog-wide backfill, auto-consistent through re-enrichment (gitea-#190); a MusicBrainz release-year lookup for rows whose tags lack one — high-confidence-only (score ≥ 90 + artist match), live kill switch, never overwrites, the first deliberate carve-out from "embedded-tag metadata only" (gitea-#208); Year/BPM/Energy catalog columns behind a visibility toggle + `?year=`/`?decade=`/`?year-missing=` filters; track duration on the now-playing card (elapsed/total + progress) and history lists (gitea-#218); scheduled-enrichment gitea-#191 closed by documentation — the scan interval is already live-editable and claim predicates already touch only new/incomplete rows.
- **Epic Y — Curation & settings fidelity (SPEC F52–F56)** — precise eligibility curation: `GET /api/media/facets` (distinct artist/album/genre values + counts) feeds exact-match filters (`artist-exact`/`album-exact`/multi `genre-exact`) on browse and all bulk operations through one shared WHERE builder, so "take this artist out of rotation" never sweeps a substring lookalike (gitea-#189); inclusive ceilings on every live numeric setting, boot deliberately untouched (gitea-#221); voice + persona dropdowns via a per-key settings control registry (gitea-#224/gitea-#225); ten initializer-equal defaults seeded so no settings field renders a lying blank, help text at full allowlist coverage with a three-way parity guard (gitea-#230/gitea-#231); the ArtistSeparation/RecentWindow coupling documented with a live inline notice (gitea-#227); plus a root-cause fix for boolean settings rendering unchecked while on (the JSON config provider's `"True"` vs a case-sensitive compare).
- **Epic Z — Ranking & robustness (SPEC F57–F64)** — artist/album ranking via bulk vote/never-play on the Catalog toolbar (gitea-#233) with flyover help on every icon control (gitea-#234); feeder-ring integrity: metadata lifetime decoupled from the anti-repeat ring + remember-at-push (gitea-#219/gitea-#220/gitea-#229); scan availability grace via consecutive-miss threshold (gitea-#223); enrichment worker-pool pruning (gitea-#222); year lookup prefers the oldest qualifying recording (gitea-#228); compose-env test drift guard (gitea-#235); LUFS gate recalibrated to target − program offset (gitea-#204).
- **v2.0.0 — Spectator Mode (SPEC F60–F66)** — the station gets a public face: a read-only, unauthenticated, cacheable spectator surface (now-playing, play history, stats, about) behind a **fail-closed authorization seam** (named `AdminOnly`/`Spectator` policies, deny-all fallback, `SurfaceGateMiddleware` 404 semantics) and a **dedicated public Kestrel listener** (`:8081`) that structurally cannot reach admin, media, or engine routes. Four operating modes (Operator/Standard/Appliance/Headless) from two flags; disclosure-by-construction payload contracts; static spectator page in the "Wireless" identity; tag-derived version stamping + `release.yml` publishing signed images on `v*` tags; the reference appliance topology (Caddy front door) live at demo.genwaveradio.com.
- **v2.1.0 — Sound & Stability (SPEC F67–F77)** — the Q3 sweep: public-surface **privacy invariants** with a CI host-publish guard (born of a live icecast `:8000` exposure, hotfixed then made a testable invariant); one **speech normalization chokepoint** (reasoning-block scrub, markdown strip, operator pronunciation corrections with live preview); **LLM degradation modes** Normal → Soft → Hard with auto walk-down/up plus an operator pin — LLM failure is a mode, not an error; **Piper TTS fallback** so a dead Kokoro is inaudible; the **persona foundation** (cards, quirk sampling, persona memory with anti-repeat, the DJ clock in every prompt); the **booth log** (Postgres narrative feed answering "what did the DJ do at 9:14", 14-day retention) + **LLM call inspector** ring; speech **boundary deferral** (idents wait for track boundaries, supersede-by-kind) with boundary-aware selection bias; `annotate:` completeness; MusicBrainz etiquette (1 req/s, miss-stamping); and the Cloudflare tunnel connector brought in-repo as versioned, observable infrastructure (F77).
- **v2.2.0 — Observability expansion (SPEC F78)** — the fleet becomes observable end-to-end: a **Grafana Alloy log shipper** behind the off-by-default `logging` profile (pinned image, fail-loud on a missing push target, docker.sock read-only under a dedicated CI guard that survives the `/run` vs `/var/run` spelling), shipping every container's logs with exactly three indexed labels (`service`/`station`/`env` — `observability/LABELS.md`); **dashboards as code** (`observability/grafana/`, GenWave Ops board, `allowUiUpdates: false`, contract-labels-only queries spec-enforced); **`launch.sh` presets** — bare dev flow unchanged, `--pinned` for published-image boxes (pull → migrate → up, never builds), `--with a,b` profile merge — collapsing the demo box's manual upgrade sequence to one command; and the **Zero Trust access pattern** documented in DEPLOYMENT.md (humans 302 to Cloudflare Access with Google + one-time-PIN, machines present service tokens that 403 at the edge without credentials), demoting SSH tunneling to break-glass. Proven live: home-lab Loki/Grafana receiving the demo station's logs through an Access-gated tunnel — where the pipeline's first catch was a misconfigured connector in its own rollout.
- **v2.2.1 — Piper probe etiquette** — the health probe uses OPTIONS so healthy Piper servers stop logging ValueError tracebacks (gh-#64); demo ollama fence corrected to 6GB after live OOM kills on the 3GB fence.
- **v2.3.0 — Personalities on Air (SPEC F79–F85)** — the DJ gets taste: **segment envelopes** filter candidates by construction (genres/energy, relax-ladder with loud logs, never-silence), the **persona ranker** biases inside them (authored/operator/accrued rules with day/hour gates, softmax Top-K, a code-enforced 5% exploration floor — "the envelope filters; the bias ranks"); **taste learning** via thumbs with every guardrail landing in the same change (±0.2 clamp, durable idempotency ledger, cap-50 weakest-evict, authored/operator exempt, structurally no ranker write path to `persona_taste`); **persona portability** (export/import cards carrying authored taste, cross-station round-trip proven, accrued rules never travel); **mood tagging** (LLM second-tier enrichment batch, fixed vocabulary, misses stamped like hits); fired rules reach the copywriter prompt as optional color. The ollama DJ-brain ships behind the demo overlay with a 1 CPU / 6GB fence.
- **v2.3.1 — Access-aware hardening** — login outcomes log the caller's identity, including the Cloudflare Access header when present (gh-#74); persona preview declines 503-Busy instead of queueing behind on-air renders; measured TTS items stamp `liq_cross_duration` (gh-#80); and the DEPLOYMENT.md **doc-drift CI gate** is born (gh-#77) after the ollama fence doc lagged the compose value by two releases.
- **v2.4.0 — Persona visibility (SPEC F86)** — "why did it play that?" answered on every surface: track rows stamp `booth_log.pick` (fired-rule summaries + exploration flag, from the SAME `PickResult` the copywriter reads, F83.1), rendered as **rule chips XOR an exploration badge** on the booth log and the Live now-playing card — the same stamped row the taste thumbs target, so the two can never drift; a per-persona **taste inspector** (source-grouped rules, signed weight bars, accrued-vs-cap meter); catalog **moods column + `?mood-exact=` filter**; spectator-disclosure pins prove none of it leaks publicly. Plus the feeder backlog death-spiral fix (gh-#88): refill no-ops keyed on feeder-OWNED ids, ending the unbounded engine queue observed live (345 requests).
- **v2.4.1 — Publish the whole appliance** — `release.yml`/`ci.yml` publish the admin UI and engine images alongside api + icecast (gh-#93), so a `--pinned` box pulls all four from GHCR; with the render budget raised to 120s this is also the release that unfroze the demo's patter loop.
- **v2.4.2 — Station IDs in the station voice** — idents are the *station* speaking: station IDs always render with the station voice, never the active persona's (gh-#96); persona attribution stays on lead-ins, back-announces, and time-date patter.
- **v2.5.0 — Taste integrity & operability (the gh-#4…#106 backlog sweep)** — the tracker goes from 20 open issues to 8 in a day: **safe-content taste exclusion** (gh-#99) — safe-loop tracks and station IDs are functional audio, never rateable: per-row votes/never-play 403, bulk sweeps carve safe libraries out in SQL, and taste thumbs refuse safe airings *before* the accrual store is reached (a thumb was teaching the persona an artist rule for the station's own name), with every admin surface rendering no control at all — all keyed to the **live** `SafeScope` and bridged across the station/library schema-role boundary via new `ISafeScopeProvider`/`IMediaLibraryMembership` seams + `booth_log.media_id` (db/22); **taste rules can no longer die silently** (gh-#87) — null `DaysOfWeek` means "no day gate", the repository read seam normalizes `{}` contexts, and a throwing rule WARNs-and-skips instead of degrading the whole persona layer; **scripts that never leave half a stack** (gh-#19) — shared preflight (Docker installed/running/permitted, compose plugin, SDK, `.env` secrets — every failure with concrete next steps), dev-flow rollback to a clean zero, and an appliance flow that keeps broadcasting through a failed converge; **granular admin policies** `Operator`/`Curation`/`Settings`/`PlayoutRead` as the RBAC seam, zero behavior change (gh-#8); a **listener-count time series** on the event-sink spine — real samples only, never a fabricated zero (gh-#10); the **version footer** on every admin page, stamped from the release tag (gh-#7); `tools/test-pronunciation.sh` — the MacLeod respelling loop through the real preview pipeline (gh-#37); **HARDWARE.md** with confidence-coded claims, its fence value drift-guarded (gh-#20); the legacy `UpdateAsync` retired with a stays-retired contract pin (gh-#4); the F57.4 narrowing recorded at the echo-branch guard (gh-#5); the ICY `StreamUrl` album-art spike closed **viable** (gh-#14 → gh-#105; hard gate found: our Icecast 2.4.4 silently drops the `url=` param — the ≥2.5.0 bump is queued and 2.4.4 EOLs 2026-12-31); and the Story179 memo-window CI flake fixed with an injected clock (gh-#106).
- **v2.6.0 — Listener Requests & On-Air Artwork (SPEC F87–F88, gh-#105)** — the flywheel's third act, pulled forward from Q1'27: **anyone can ask the station for a song**. One anonymous, throttled free-text wish (`POST /spectator/api/requests` — the codebase's first public WRITE, fail-closed at every layer: middleware kill-switch 404 *before* the rate limiter, chained cooldown+daily per-IP limits, 8KB body cap, constant 202 that is byte-identical for matchable/unmatchable/gibberish — the request line is **not a catalog oracle**); wishes parse via the constrained-LLM mood-tagger posture (fenced data, vocabulary-filtered, deterministic fallback under degradation, wish text in zero log lines, 24h retention) and match against the real catalog under full operator law (ready + measurable + eligible + never-play + the gh-#99 **safe-scope exclusion** — a wish for "please stand by" cannot fulfill the station's own safe loop); a matched request short-circuits the pick **once per pick** (one-shot CAS, 15-min TTL, `OverrideEnvelope` live setting for request-beats-envelope vs programming-integrity), airs with a **generic request-line shout-out** folded into its lead-in (a reflection fact pins that no listener text can structurally reach a prompt), and the spectator page grows a Wireless-styled wish box. Meanwhile **players get real album art**: Icecast rebuilt from the pinned 2.5.0 source (2.4.4 silently dropped the `url=` field and EOLs 2026-12-31), per-track art served from opaque non-enumerable 128-bit tokens (only aired tracks ever broadcast theirs — "on-air identity public, catalog private" extended to art), ffmpeg-extracted/500px/immutable-cached with a no-oracle station-icon fallback, and ICY `StreamUrl` carrying it to every metadata-aware player — a defect the epic's own live acceptance gate caught (the encoder export list) and fixed on the wire. Four review-gate catches shipped fixed with regression pins; the whole epic was built task-by-task through /build-loop with an opus review gate per task.
- **v2.6.1 — One caller is not the whole internet** — patch: the reference public topology's two proxy hops each defaulted to distrust (Caddy stripping inbound XFF without `trusted_proxies`; the api's forwarded-headers walk stopping one hop short), so every public visitor resolved to a container IP and the per-IP limiters — the brand-new request line's cooldown/daily cap, the spectator 120/min — partitioned the entire audience onto one key. Found live within minutes of enabling requests on the demo (cross-IP 429s). Both halves fixed and config-pinned; the walk now runs through trusted hops to the real client, and DEPLOYMENT.md documents the post-deploy check (a login line's `remote:` must never be `172.x`).
- **v2.6.2 — A slow render is not an outage** — patch: Kokoro answers `/health` from the same event loop it renders on, so an ordinary render (median 6.9 s, p90 10.1 s per text-split on the demo box) blocked the probe past its budget, flipped the cached verdict, and quietly diverted the next patter segment to the Piper voice ~25×/day (gh-#125). No timeout can outrun that tail without going blind to a genuinely dead engine, so the verdict now **debounces**: unhealthy only after `DependencyHealth:UnhealthyThreshold` consecutive probe failures (default 2 — across 298 timeouts in a 7-day window, zero landed on consecutive cycles, while a dead engine is still caught within 2×interval). Sub-threshold misses log at Debug; a "disabled by design" probe (an empty endpoint per F34.2) is never debounced — that's a declaration, not a flap. The shipped timeout default rises 5 s → 10 s (the demo's 10 s lived only in its environment), and all three cadence knobs (`DependencyHealth:ProbeIntervalSeconds` / `ProbeTimeoutSeconds` / `UnhealthyThreshold`) are now allowlisted **Live** — the prober re-reads them every cycle and retunes its timer, no api restart per diagnostic step.
- **v2.7.0 — Persona Catalog: browse, audition, hire (SPEC F89–F90)** — the community shelf opens. A separate, CC0, PR-curated [`genwave-catalog`](https://github.com/GenWave-Org/genwave-catalog) repo (schema-validated entries, CI-built index with per-file sha256, deterministic tree-derived dates) ships stocked with **twelve first-party DJs** — Midnight Mabel to Big Tony Marconi — each merged through the public contribution flow as its own PR. The api reaches the shelf through ONE guarded door: `CatalogProxyService` (fixed origin per the operator-trusted-endpoint precedent, relative-only schema-shaped paths bound to their declared slugs, redirects never followed, size caps enforced during streaming, sha256-verified before any cache write, 15-min single-flight cache with per-slug invalidation and stale-on-failure) behind `Community:CatalogIndexUrl` — live setting, empty = fail-closed bare 404s. The Admin UI grows a **Persona Catalog** page (always-visible 18+ badges, bestFor chips, plain-text-only rendering) whose **informed-adoption review modal** is the trust ruling made clickable: the FULL card text — soul, quirks, lore, corrections, taste, samples, plus a self-healing "other fields" section — displayed with zero requests issued until the explicit confirm, which POSTs the raw card bytes; the same modal now gates **file-upload imports** too (one adoption rule, no loophole). Provenance lands in db/25 (`imported_from`/`imported_at`, catalog slug or `file`, surfaced as a Personas-page badge with a year that actually renders). Isolation is pinned structurally, not by promise (F90.8): spectator bodies anchored to their DTO shapes, catalog routes bare-404 on the public listener, a mutation-validated DI-closure walk proving playout can't reach the shelf, and a byte-pinned golden card both repos hold. Catalog log lines neutralize remote-derived strings (the CodeQL follow-through that found the scanner had flagged two false positives and missed the real one). Every task passed an adversarial review gate — all of them failed round one, which is the gate working.
- **v2.8.0 — Format Clock: On The Air (SPEC F91–F95)** — the station gets a broadcast day. A weekly schedule grid (`station.segment_schedule`, db/27, 30-minute wall-clock slots in station-local time) resolved by a pure `ScheduleResolver` behind a caching wrapper retires the single active-persona switch: **On-The-Air replaces Active** as the seam every consumer reads (F91.5/F91.7), persona delete is FK-guarded by the slots it holds, and `GET/PUT /api/schedule` moves the week as one document. DJ **handoffs are a two-piece ceremony** (F92): a boundary producer drains the outgoing DJ's sign-off and the incoming DJ's sign-on — two new copywriter kinds, each rendered in its own DJ's voice and card. The spectator page shows **who's on, who's next, and the art** (`dj`/`upNext`/`artworkUrl` on now-playing, gh-#159); the Admin UI grows the **Roster** replacing the switch, export-first **Fire** (the parachute saves the right card), the **drag-paint schedule editor**, and **Hire as the catalog's verb** with a deliberately boring contract (F94, gh-#169). Riding alongside: **audience posture** (F95, gh-#174) — `Station:Audience` (`everyone`|`mature`) on the settings allowlist, per-track explicit classification on the enrichment spine (db/26; an advisory-tag pass plus an LLM sweep, each source-labeled), enforcement as **one pool predicate** so nothing explicit airs on an `everyone` station, and an explicit badge + operator override in the catalog. STORY-240–251 landed spec-first, closed by an acceptance gate proving the clock converges.
- **v2.8.1 — Now-playing catches up (gh-#184)** — patch: the feeder published now-playing only after its refill tick, so a refill blocked on TTS renders could hold the display 40 s past the real transition; publishing **before** the refill blocks cuts live catch-up to 2–4 s, verified on the demo box. Plus first Format Clock touch-ups: chat preambles stripped before they reach the air (gh-#186), prompt guardrails on the embellishment license and quirk examples (gh-#188), the DJ break gets its own admin card treatment (gh-#187), and the Story187 live-cadence test deflaked with virtual time (gh-#171).
- **v2.8.2 — Tense contract + console help sweep** — patch: back-announce segment lines carry the tense contract outright so a just-played track is never previewed (gh-#195); spectator HEAD answers on every route GET is mapped on (gh-#160); booth-log and Personas pages get their help sweep (gh-#141–#143); two test-hygiene pins (strict `Assert.Equivalent` gh-#167, TZ + locale pinned suite-wide in jest gh-#168).
- **v2.8.3 — Scan resurrection + polite enrichment** — patch: an unavailable row resurrects on any scanner sighting, closing the gap where a recreated media dir parked tracks forever (gh-#112); loudness-measurement children run at idle priority so enrichment stops competing with the broadcast encoder (gh-#38).
- **v2.8.4 — The on-air voice trio** — patch: **they/their for artists — never infer gender from a name** (gh-#151), DJs occasionally mention their own name (gh-#150), and personality-neutral boilerplate only when no persona is active (gh-#152). Plus the `Tts:Corrections` editor made trustworthy — edits persist, staging is unmistakable (gh-#139/gh-#140); the spectator player recovers from stalls instead of Chrome's Range-resume loop (gh-#114); the voice-warning hook uses the stored persona slug, not a name-derived one (gh-#179); engine `clock.leak_warning` raised to 150 — patter-cycle churn is GC-lag, not a leak (gh-#185); and persona names are CRLF-stripped before logging (CodeQL log-forging).
- **v2.8.5 — The station's clock, the station's logs** — patch: `Station:Timezone` — the LLM's sense of time follows the station, not the container (gh-#117); Icecast access+error logs stream to stdout/stderr for the Loki tailer (gh-#115); settings UX — a structured kind-by-engine editor for `Tts:EngineByKind` (gh-#146) and help moved to a flyover on the field title (gh-#145).
- **v2.8.6 — Pickable wishes + catalog hygiene** — patch: the request form gains genre/mood pickers backed by a real genre predicate (gh-#131); unavailable rows hide from the catalog behind a count + reveal toggle, with an explicit operator purge for long-unavailable tracks — dry-run first, outage tripwire so a dead mount can't mass-delete (gh-#113); the public surface gets CSP + security headers, `img-src` following `PublicBaseUrl` live (gh-#180).
- **v2.8.7 — Station Imaging (gh-#149)** — patch: "Safe content" grows up into **Station Imaging** — authored segments carry a content kind (Liner, Station ID, Jingle, Promo); the Admin UI gains a **Health page** (container stats via an allowlisted socket-proxy sidecar, gh-#148) and per-area settings tabs (gh-#144); the schedule grid and taste gating follow `Station:Timezone` (gh-#224); booth-log MODE explained, ELAPSED humanized, and the taste ACCRUED value explained (gh-#209/gh-#210).
- **v2.8.8 — Multi-arch: the Pi becomes a target** — patch: release images publish **amd64+arm64** (gh-#240), including a repo-owned multi-arch piper image that preserves the text/plain wire shape (gh-#241); a **piper-only compose override** boots without kokoro on low-memory boxes (gh-#242); HARDWARE.md gains its Raspberry Pi section from the gh-#213 spike (gh-#243).
- **v2.8.9 — Editor save-loss + honest attribution** — patch: the schedule editor's stale-week silent save-loss closed with a version guard (gh-#255) and the persona editor stops wiping catalog cards — soul/quirks/lore visible, never destroyed by an unrelated edit (gh-#256); spectator Now Playing attributes the DJ **from the airing item, not the schedule**, pinned end to end (gh-#259); DJ-break art gets a card-sized station mark (gh-#258); HTML entities decode once at tag ingest (gh-#257); the kokoro mem cap rises 3g→4g — spike headroom over the leaked baseline (gh-#276); CI stops moving `home-latest` on merges to main — `release.yml` owns it (gh-#274).
- **v2.8.10 — Land the handoff near the boundary** — patch: a **patter-duration estimation seam** with honest confidence tiers (gh-#253) feeds **boundary-fit selection** — the last pre-handoff track is picked to land near the boundary instead of straddling it (gh-#254); `Tts:Fallback` generalizes into an ordered resiliency chain (gh-#147); **heteronyms become expressible** via context-conditioned correction rules, with context columns in the corrections editor and the shape guard accepting the new fields (gh-#161).
- **v2.8.11 — Kokoro breathes** — patch: engine-aware `[pause:0.6s]` inserted at sentence boundaries on the Kokoro request path, per-engine wire split and cache-key safety pinned (gh-#116); the exploration taste note reworded and gated to lead-ins only (gh-#270); the artwork fallback is cacheable but never immutable (gh-#258 follow-up).
- **v2.8.12 — Live means live** — patch: the spectator player's pause **detaches the live source** and resume rejoins the live head with a fresh re-arm — no replaying a stale buffer, and the native play button survives the detach (gh-#298); a fired dislike is never spoken as a taste match (gh-#291); compose-render specs pin `COMPOSE_PROFILES` empty so the suite is hermetic (gh-#249).
- **v2.8.13 — Pi first-boot unblocked** — patch: the two blockers found standing up the Raspberry Pi 5 — `launch.sh --pinned` can now first-boot a fresh box (gh-#305), and piper-only drops the LLM pair while bare compose matches the launch (gh-#309/gh-#310); **the last unit before a ceremony IS the ceremony** — no more declined final units (gh-#300, deliberately scoped as the bottom rung of three, gh-#320); **commas banned in LLM patter** — the prosody stumble fixed at its source (gh-#303); five dependency bumps (Next 16, react-dom, ts-jest, shadcn, docker/login-action).
- **v2.8.14 — Speech markup + pronunciation model (SPEC F96–F97)** — patch: the "Make the DJs sound human" epic's first rungs land behind pending BDD specs (STORIES 252–258): the **engine speech-markup stage** — Kokoro composes markup, Piper strips it, an unsupported form is never a render failure (T133, F96) — and the **pronunciation rule model + compiled matcher** (`{pattern, word, ipa}`) (T135, F97, gh-#161).
- **v2.9.0 — Pronunciation reaches the wire (SPEC F97–F98, gh-#161)** — the DJs learn to say the words: pronunciation rules resolve **where the persona is known** (T137), with **persona-beats-station precedence pinned at the ordering altitude** (T136, gh-#284), and travel to the engine adapters on `TtsRenderContext` alongside the persona's speaking pace (`VoiceSpec.Pace`, F98) (T134) — heteronyms included, thanks to the F97 `pattern`/`word` split, with every fired rule logged at Information so the operator can see why "wind" came out right.
- **v2.9.1 — Small levers for small boxes** — patch: TTS probe failures WARN on the **failing edge**, not on every cycle after it (gh-#338); enrichment concurrency gains a lever, halved under piper-only (gh-#334); `launch.sh --pinned` shows the stopped containers when a converge fails part-way (gh-#332); HARDWARE.md records the **Pi 5 verified on ARM** — data tables first, steps 4 and 5 measured on the box itself (gh-#213, gh-#307/gh-#308/gh-#336/gh-#339).
- **v3.0.0 — Themes: the station gets a wardrobe (SPEC F102–F103, gh-#206 first slice)** — the whole theming epoch in one release. **Layer A (F102)**: a theme is one JSON manifest — colour tokens for light *and* dark plus curated font references — parsed once (`ThemeManifestParser`, parse-is-validation, CSS-injection-hardened token/family/src patterns) and composed by one `ThemeCssComposer` into `/api/theme.css` + `/spectator/theme.css`; both consoles' hand-maintained token blocks retire; resolution runs visitor cookie → `Station:Theme` → shipped default, under a **data-driven WCAG-AA contrast gate** over every shipped palette. **Layer B first slice (F103)**: the community catalog learns a second **kind** — the persona shelf generalises to `{manifest, meta}` entries routed by `kind` (an unknown kind skips, forward-compat for fonts/icons/avatars), theme cards paint **preview swatches straight off the one index read** (the `bestFor` precedent — no per-card fetch, ever), the detail view composes an **honest live mini-preview through the same composer that serves the live payload** (ancestor-form scoped selectors so the operator's explicit light/dark choice wins in all six mode combinations; live path sha256-byte-identical), and install flows through the F79 import shell — `POST /api/themes/{slug}/import` with a bounded read, a fail-closed `schemaVersion` gate, shipped-slug 409, `catalogSlug`/`file` provenance surfaced as rows on Settings, and a transactional upsert + live catalog rebuild that survives a mid-request client disconnect. **Preview refuses whatever import would refuse.** Curated fonts become a **governed process** (FONTS.md: OFL-confirm → machine-readable provenance record → latin-subset → a 200 KiB per-theme ceiling, enforced by a vendored-face validator at boot AND import) and the set grows to five faces — **JetBrains Mono and Grenze Gotisch** join Fraunces/Source Sans 3. The app now **embeds exactly two themes** (Cat's Whisker + Test Pattern — the offline floor that resolves with no DB and no catalog) while **four move to the community catalog as its first theme entries** — Wow & Flutter, Graveyard Shift and Pirate wearing Grenze Gotisch, Skip wearing JetBrains Mono — F102.1 amended to "≥6 across app + catalog". Isolation is pinned structurally (F103.12): every catalog/theme route lives on the admin surface, spectator payloads are byte-identical with the catalog enabled, disabled, unreachable or the DB absent, and the shelf stays fail-closed on an empty URL. Every task shipped through /build-loop's adversarial review gate (seven first-round FAILs, all recursed to PASS) plus a production smoke, and the full browse-install-wear journey was rehearsed end-to-end against the real seed catalog before this tag existed.
- **v3.1.0 — Font packs: the library opens (SPEC F104, M1 of the wardrobe workshop)** — the community catalog's third **kind**. A **font pack** is 1–2 OFL-licensed, latin-subsetted variable faces plus the licence text itself, hosted **in** the catalog as its first binary assets — every file sha256-pinned in the index, every gate (schemas, a 200 KiB pack ceiling, licence allowlist, stowaway/orphan/duplicate checks, slug-ownership cross-checks) enforced by the catalog's own CI, and packs are **Dean-curated only** by standing ruling. Station-side, `POST /api/fonts/{slug}/install` fetches every declared asset through the guarded door (the pack ceiling enforced *during* the fetch loop — a hostile hand-edited index gets cut off at the tipping asset, not buffered), verifies every hash, and lands the faces in a new **`station.font_pack` library store** (db/32) in one transaction; `/fonts/{file}` widens from five vendored literals to a **closed set** of vendored ∪ installed faces served from a volatile in-memory snapshot that **keeps serving with the catalog and the database gone** — the F102.7 offline philosophy extended to typography. The Admin UI grows **font cards on the community shelf** (family as the title, byte total, zero fetches on browse), a **detail specimen that renders sample text in the real hash-verified face** and then discards every byte on close (object-URL + FontFace lifecycle, review-probed to zero residue), the T186-style confirm-install flow, and a **Library page** listing installed packs with faces, licence, and "Installed · ⟨slug⟩ · ⟨date⟩" provenance — deliberately reachable with the catalog disabled, because your installed inventory outlives the shelf you got it from. Seeded by **Space Grotesk** (upstream 2.0.0, OFL records retained in the binary via the amended `--name-IDs='*'` recipe, byte-reproducible). Every task shipped through /build-loop's adversarial review gate; the recurring catch of the epic — gates looser than the app they mirror, and fixtures that couldn't discriminate the property they guard — is why the reviewers now mutation-test every fix before passing it.
- **v3.1.1 — Wardrobe polish (gh-#375)** — patch: Dean's T204 demo feedback closed on both catalog kinds. Fonts gain the pre-install trust surface they lacked — `CatalogEntryResponse` now projects licence/version/subset (all six font args passed BY NAME, the null-soup guard), and `FontDetailPanel` renders the same "licence · version · subset" line the Wardrobe page uses. Both kinds get honest installed-state truth: `/api/fonts` rides the page load (fail-closed to `[]` — documented direction: a stale "Install" costs an idempotent re-install, a false "Installed" costs an operator a missing font), an Installed chip + Re-install label, and a successful install flips state locally while a failed one flips nothing; themes add the matching "Imported · ⟨source⟩ · ⟨date⟩" provenance sourced from the existing `/api/settings` `Station:Theme` choices (no new route). Plus the naming ruling: `/library` becomes **`/wardrobe`** (nav label, `WardrobeClient`/`WardrobePage`/`WardrobeIcon` — wire contracts stay put, UI labels only).
- **v3.2.0 — Wardrobe workshop M2: the editor (SPEC F104, PRs #395/#396)** — the theme font law widens to **vendored ∪ installed** (T205): `ThemeFontProvenanceValidator`'s per-theme byte ceiling now sums both sets, and an import 400 names the missing face plus, when the catalog index knows a pack that provides it, the pack's own slug (fail-soft — catalog CI stays curated-only, so shipped themes remain structurally incapable of referencing a pack). The **theme editor** (`/editor`, T206) mixes a base theme's palette with a role face (display/sans) over one server-derived assignable set (`GET /api/fonts/assignable`, vendored ∪ installed in one union), previewing through the T186 transient-compose mechanism — nothing persists until **save-as-own** (T207) writes it to `station.theme` behind a shared `ThemeWriteGate` (one two-phase pipeline now serves both write routes, gate-identical by construction) with a **fail-closed 409** refusing to overwrite an already-imported theme's provenance. Font pack **uninstall** (T208) removes a pack unless a saved/imported theme still references one of its faces — the guard lives *inside* the `DELETE` statement itself (`WHERE NOT EXISTS`), not an advisory pre-check a concurrent write could race past; refusal names every referencing theme. T209 re-audits public disclosure over the widened surface (self-derived AdminSurface+Settings sweep, the one sanctioned spectator change pinned). T210's exit-demo — mix, save, activate on the pinned v3.2.0 demo box — closed F104.
- **v3.3.0 — A station, not a playlist (SPEC F107–F113, project #3's flavor floor)** — the content kinds radio is made of, on one seam. **The context seam (F107)**: `IContextProvider` joins the MIT contract surface (the demonstrated F105.6 need is GenWave Business) with implementations in the new **`GenWave.Context`** project — born outside Host per the F105.4 graduation rule, covered by every fitness law from day one. One pipeline (fetch-once-per-cadence-slot, freshness re-checked on every read, **skip-never-silence** — a dead provider is inaudible), two delivery lanes: rendered **context segments** at track boundaries via the F74 queue (supersede now per `(kind, discriminator)` so due weather never erases due history) and an **at-most-one, cadence-gated context fact** in LeadIn/BackAnnounce prompts — byte-identical prompts otherwise, golden-pinned. Provider text is **fenced as data-not-instructions** end to end: the sanitizer makes fence delimiters structurally unrepresentable (adjacent identical angle brackets cannot survive it), lengths capped, newlines dead — adversarially proven against crafted Wikimedia payloads. **Weather (F108)**: Open-Meteo, keyless, SSRF-closed (validated invariant coordinates into a fixed host — nine injection shapes fail closed with zero outbound requests); the 2026-07-20 spoken-vs-precise ruling enforced — coordinates reach no prompt, payload, or log line, `Station:Location:SpokenName` is the only place name a DJ may speak; blank coords = provider off even when enabled, edge-logged once. **This day in history (F109)**: Wikimedia On-This-Day (curated `selected` feed), **paraphrase-only** under the news posture — a 3B model never free-recalls dates on air; day-file cache + next-day pre-fetch so the segment survives restarts and outages; F76-style etiquette UA shared with MusicBrainz. **Idents & time (F110)**: opt-in clock-anchored top-of-hour idents drain **pool-first** — the gh-#149 imaging collection gets its first playout consumer (random ready authored `station_id` row airs as audio; templated TTS fallback), and `SegmentKind.TimeDate` gains its first-ever producer — *"It's two o'clock."*, station-voiced, zero LLM, per-hour text = forever-cache hit; F95.4 tightened along the way (an explicit-marked ident never airs on an `everyone` station — fail-closed to the template, never dead air). **The straddle handoff (F111, gh-#320's middle rung — and gh-#300 closed with it)**: when nothing in rotation fits the boundary and the picked track **provably crosses** (measured duration only — never inferred), the outgoing DJ signs off *into* it and the incoming DJ picks up on the far side, back-announcing the captured track; ceremony-now demotes to a genuine last resort. **The pick pipeline left the Orchestrator first (F112, gh-#401)**: `MusicSelectionPolicy` owns request rung → persona rung → trust-but-verify → degradation ladder → boundary resample, returning a `Fit | Straddle | CeremonyOnly` outcome with **one floor authority** and rung-tagged fit logging (F111.5). **Airings became countable (F113)**: `booth_log.segment_kind` stamped at *air* time (a budget-dropped render never counts; db/33, which also lands the dormant `station.show` schema for the dayparting slice) — the epic's demo-hour gate is one indexed query, shipped as `tools/demo_hour_gate.sql`. Built via /build-loop's adversarial gate: **every substantive task failed round one** and every catch was mutation-proven before passing — the sim that showed 70–92% of hourly idents silently erased (now 0.0%), sign-offs airing 4–6 minutes early on short-track pools, and a prompt fence a Wikimedia editor could close from the outside.
- **v3.3.1 — The whole sentence airs (gh-#424)** — patch: cue-out now comes only from silence that **actually extends to EOF**. `FfmpegCueAnalyzer` took the *last* silence region as cue-out unconditionally, so an interior pause that happened to be last — a 0.6 s injected TTS sentence gap (they clear the 0.5 s silencedetect floor), a quiet mid-track break — became `liq_cue_out`, and **the final sentence of every multi-sentence patter clip was cut on air** while booth logs, the cached wav, and the preview all looked complete (`DurationMs` derives from the same cue-out, so even the progress bar agreed). Trailing now means open-ended at EOF or closed within 0.25 s of the container duration; interior-only files report the full audible extent so durations stay measured for straddle boundary-fit; misclassification degrades safe-only (untrimmed tail → `blank.eat` backstop). Analyzer-level on purpose: music with a quiet interior break stops mis-trimming too. Playout self-heals on deploy (blurb cues re-measure per process); persisted cue rows — music enrichment, authored imaging — keep old values until re-enriched/re-authored. Found in Dean's T229 listening pass (both v2.9.0 and v3.3.0 sightings were this one path); regression spec red-checked against the pre-fix parser.
- **v3.3.2 — Nothing grim, nothing clipped (gh-#433, gh-#424 rollout)** — patch, two halves. **The history airability gate (gh-#433)**: `HistoryFactHygiene` sits between the On-This-Day day cache and either delivery lane at **vend** time — somber facts (violent-death / disaster / atrocity vocabulary, word-boundary matched, deliberately fail-closed) never air, and half-stripped `[[wikilink]]` bracket residue is cleaned from what does. Driven by a live sighting: a fatal plane crash aired as chill-morning patter color, stray bracket included. An all-somber day — Wikimedia's curated `selected` feed skews grim; the real captured reply was five-for-five — is an ordinary F107.6 skip, one Information line; pool-widening to the fuller `events` feed is the recorded follow-up if coverage thins too far. **The stale-cue retirement (db/34)**: every cue point persisted before v3.3.1's analyzer fix could carry a mid-file `cue_out` (music with a quiet break clipped at the pause — heard live on the demo). A marker-guarded one-time migration (`library.one_time_fix` ledger, claim-CTE, twice-run-proven against a real postgres:16.4) nulls the stale cue columns at deploy — instantly curative, NULL cue = full-file playback — and the F13 backfill predicate re-measures the whole library with the fixed analyzer at the worker's own pace. Zero operator action, per the standing zero-manual-release-steps rule.
- **v3.4.0 — Dayparting: named shows (SPEC F115–F121, gh-#383)** — the hours get names. **Shows are identity packages** (`station.show` widened, db/35: slug/tagline/flavor/provenance + the dormant schedulable-bundle columns, ruled unread until that slice designs their readers): authored on a new **Shows page** (60/120/400 budgets, guarded delete naming referencing blocks), assigned from the schedule grid's side panel with **run-span default** — one click names a whole contiguous same-persona run, and music-only runs span too (ruled: one click names the overnight stretch). **The ear knows**: sign-on ceremony carries the incoming show's name + flavor, a same-DJ show flip airs exactly one transition piece (F92.3 dedupe now keys persona AND show), a **cadence-gated flavor line** shares F107.5's single extra-line slot (context facts win; the prompt ceiling never grows), and top-of-hour idents go **show-scoped-first** — authored rows scoped to the show, then the station's authored pool, then a templated "You're listening to ⟨show⟩ on ⟨station⟩." floor (ladder ruled: authored beats template). **The public face knows**: spectator now-playing carries `show {name, tagline}` and `upNext.show` (name-only by type shape; flavor is prompt-config, structurally absent from every public payload — value-level pinned). **The catalog grows its third community kind**: `show` entries (schemas + budget lint + 12 seed cards, one per demo DJ, shipped as 12 one-entry PRs per the moderation model) import through the F79 shell with an **atomic authored-protection upsert** (`ON CONFLICT … WHERE imported_from IS NOT NULL` — the gh-#394 conditional-write form; an authored show's slug survives any import byte-for-byte) and a **soft "also hire ⟨DJ⟩" offer** that opens the persona's own full-card review (never a silent second import); imported Name/Flavor are sanitized at the parser seam (control chars flattened, fence runs collapsed) with the F90 full-card confirm as the ruled trust gate. **Dated specials land as the ruled-droppable tail, delivered** (db/36, own migration): per-date EXCLUDE-guarded one-offs shadow the weekly grid for their span with honest boundaries both directions (mid-block resume; a midnight special still arms the outgoing sign-off via the tomorrow-boundary-peek), authored on a dated-list form — deliberately not a second paint grid. **Airings are show-countable** (`booth_log.show_id`, air-time stamp, no FK) — the demo-hour gate gains its show halves as one indexed query. Rider: **time announcements join the ident** ("It's two o'clock on ⟨station⟩." — gh-#453, ruled on the first live listen; the forever-cache re-keys by construction). Built via /build-loop's adversarial gate across five stacked PRs: catches included a grid-save wipe of every show assignment (killed at three layers), a published-NuGet binary break, a boundary-fit estimator corruption, and an upNext erasure at exactly the boundary this epic ruled real — plus two display bugs only the live wire smokes caught.
- **v3.5.0 — Every kind in its place (gh-#372, gh-#393, catalog#33 prep)** — the mixed piles get shelves. **The Wardrobe silos by kind** (gh-#393): Personas | Themes | Fonts | Shows, URL-driven tabs with every tab present even when empty (ruled on the issue: an empty kind shows its own empty state, never a hidden tab); fonts keep their founding cards + uninstall, the other kinds list genuinely-imported rows only (`importedFrom`-gated, the two-provenance-class rule) under their own verbs — Hired / Imported / Installed — with actions deliberately staying on each kind's own page. **The community shelf gets the same treatment** (gh-#372): `?kind=` tabs over the catalog grid — a persona card and a show card were visually identical without opening them — while the cross-kind machinery (the soft hire offer including its failure path, the install modals, imported-state maps) keeps reading the full index untouched. The tab strip itself is extracted once (`components/ui/tab-strip.tsx` — CatalogTabs and BoothLogTabs were already byte-identical copies; four consumers now share one). **The validator learns the catalog's next layout** (genwave-catalog#33): `entries/<kind-plural>/<slug>/` admitted alongside the flat layout — the kind folder, when present, must name the entry's own kind; slug ownership reads the second-to-last path segment; and a new one-directory invariant pins an entry's manifest/meta/assets to a single directory (what keeps bare filenames unique for the filename-keyed asset route once one slug can legally exist under two layouts). The catalog-side move (genwave-catalog#50) holds until this release is on the box — an older app rejects a migrated index wholesale, fail-closed.
- **v4.0.0 — Make the DJs sound human (SPEC F96–F100, F123–F126 · the wince-list, closed)** — the epic that resumed the parked voice work and shipped it as five slices. **Copy fits its break** (gh-#277): the completion request finally carries a generation cap derived from `Llm:MaxCopyChars` — killing the 30–87s free-running holds — and over-length copy is trimmed to the last complete sentence (abbreviation-aware, so "a brand new one from St." can never air) instead of rejected, which turns the SignOff/SignOn/ContextSegment over-length *silence* edge into aired persona copy; the prompt states its own word budget. **The boundary respects the backlog** (gh-#469): a queued tail crossing a schedule boundary now classifies as a straddle and *holds* the incoming DJ's sign-on behind a not-before gate honored by both the drain and the planner's peek against the real clock — the 22:00Z inversion where a sign-on aired over six minutes of buffered outgoing content is structurally dead — and a `TimeDate` deferral that would drain past its armed hour expires with a WARN rather than announcing a dead hour. **Storms stay somber, facts rotate** (gh-#468): the tone gate gains the wind-storm noun family (a tornado touchdown will never again air as chill-morning color), and context facts rotate at vend time against a day-scoped aired-set instead of the provider handing out the same `chosen[0]` all day — which reshaped `ContextContent` in the published `GenWave.Abstractions` package to carry the airable fact *list*, **the breaking contract change this major version marks**. **Pronunciation rides the wire**: heteronyms and names say themselves right via engine-adapter `[word](/ipa/)` markup (canonicalized at one seam), persona **pace** reaches Kokoro's `speed` (11 of 12 catalog personas ship a non-1.0 pace ignored until now — stations sound different, by design), and rule/correction hits plus per-persona render outcomes are finally visible at Information in Loki — *fired means aired*, never annotated-then-failed, with engine timeouts no longer vanishing from the failure rate. **The pronunciation editor**: an operator who doesn't speak IPA teaches the DJ a word entirely in the browser — a content-addressed rules API, a rows-not-JSON editor, a **"Hear it"** audition that plays the rule being authored through the real air chain (auditions excluded from the hit counter), an **espeak-ng respell oracle** (type "muh-KLOWD", get IPA — argv-only and option-inert after a review caught a key-ring-reading argument injection), all held shut by two new fitness laws (L7 the context-overload seam, L8 the one resolve seam). Built task-by-task through /build-loop's adversarial review gate across twelve rounds: catches included a hold that lasted zero seconds, a nested form real browsers silently strip, a CWE-88 file read reproduced in-container, and a preview surface that lied about the rules it auditioned. The voice contract (right-voice-or-no-speech, opt-in failover) and the manual ear-gate are held to a dedicated session.
- **v4.1.0 — Bug Bash Thursday (gh-#491 and seven friends)** — a one-day sweep: eight issues root-caused, fixed, reviewed, and merged same-day. **Rules beat corrections, loudly (gh-#491)**: the headline — seven identical-sounding auditions on the demo box turned out to be a legacy `MacLeod → Maa-cloud` speech correction rewriting the word out of the text before the pronunciation rule could ever match it (corrections normalize above the markup seam by design, so the rule was equally dead on air, not just in the editor — and the first audition "sounding right" was the *correction* doing the respelling). Ruled and shipped: `RuleOverCorrectionPrecedence` states the invariant once — a correction whose `From` names a rule's `Pattern` (case-insensitive identity, deliberately not containment) is suppressed for that render, one Information line per suppression in Loki, suppressed corrections never counted as fired; the rules API warns at authoring time (`{rule, warnings}` write response — never a refusal, since tweaking a rule that replaces a correction is legitimate mid-migration state) and the editor toasts it in the new warning tier. **The tone gate knows plurals (gh-#479)**: the 13 pre-existing somber nouns gain their plural forms — "two earthquakes struck…" no longer airs as chill morning color. **Provenance can't be raced (gh-#394)**: save-as-own becomes one atomic conditional upsert (`ON CONFLICT … DO UPDATE … WHERE imported_from IS NULL`, the ShowRepository form) — the read-then-write TOCTOU is gone, the 409 contract byte-identical, proven by Postgres-backed specs with no pre-check read anywhere. **Pace warns at import (gh-#483)**: an out-of-range persona pace still renders clamped, but the import response now says so ("Pace 3.0 is outside this engine's [0.5, 2.0] window and will render at 2.0"), bounds single-sourced from `TtsPace`. **The respell assist hides before the dead-end (gh-#487)**: `GET /api/pronunciations/derive/available` probes the espeak oracle latch on mount, so a piper-only box never shows a button whose only future is one 501; a failed probe assumes available — a network blip must never hide a working assist. **Old storms stop reading as live (gh-#490)**: Health cards pair Docker's monotonic `RestartCount` with recency ("10 restarts · last 4d ago"), danger styling only inside a documented 24h window — the demo box's four-day-old crashloop ghost finally reads as history. **The contract ships its docs (gh-#417)**: `GenWave.Abstractions` packs its XML documentation file — 62 missing or dangling doc entries across 26 files written properly, IntelliSense for every NuGet consumer. Plus four stale `RequestCompletionAsync` prose references (gh-#477) and a full tracker relabel (every open issue now carries kind + priority labels). Root-caused through the fleet's own logs — the api's correction-fired lines joined against kokoro's received-text lines answered what seven identical WAV sizes were hiding — and wire-verified on rebuilt images against the original field scenario before the tag.
