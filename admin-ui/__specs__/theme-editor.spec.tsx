// STORY-286 — The editor mixes components (SPEC F104.11, F104.12 · PLAN T206)
describe("Feature: the editor mixes components", () => {
  describe("Scenario: component mix only", () => {
    it.todo("assigning a face to a role re-composes the scoped live preview (T206, AC1)");
    it.todo("role pickers offer vendored plus installed faces and nothing else (T206, AC1)");
    it.todo("no token-level colour editing surface exists (T206, AC1)");
  });
  describe("Scenario: the remix is ephemeral", () => {
    it.todo("closing or reverting persists nothing (T206, AC2)");
    it.todo("only transient compose calls appear on the network (T206, AC2)");
  });
});
